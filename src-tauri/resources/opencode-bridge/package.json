{
  "name": "@tempest/opencode-bridge",
  "private": true,
  "type": "module",
  "description": "Node sidecar that drives OpenCode via its local HTTP+SSE server (`opencode serve`) and streams NDJSON events to Tempest. Run via system node; not part of the Vite/renderer bundle. No external deps."
}
