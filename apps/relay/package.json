{
  "name": "tempest-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object frame router for Tempest mobile pairing.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke": "node smoke.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "ws": "^8.18.0"
  }
}
